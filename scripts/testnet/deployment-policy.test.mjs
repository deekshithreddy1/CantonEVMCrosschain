import assert from "node:assert/strict";import test from "node:test";import path from"node:path";import{assertNonProductionChain,loadTarget,redactUrl,requireConfirmation}from"./deployment-policy.mjs";
const root=path.resolve(import.meta.dirname,"../..");test("testnet manifests are explicitly non-production",async()=>{for(const file of["sepolia.json","canton-devnet.json"]){const value=await loadTarget(path.join(root,"config/testnet",file));assert.equal(value.allowProduction,false);}});test("production EVM chains are always rejected",()=>{for(const id of[1,137,42161,10,8453])assert.throws(()=>assertNonProductionChain(id),/prohibited/);assert.doesNotThrow(()=>assertNonProductionChain(11155111));});test("deployment needs exact environment confirmation",async()=>{const t=await loadTarget(path.join(root,"config/testnet/sepolia.json"));assert.throws(()=>requireConfirmation(t,"deploy-mainnet"),/confirmation/);assert.doesNotThrow(()=>requireConfirmation(t,"deploy-sepolia"));});test("RPC evidence never retains credentials or query secrets",()=>{assert.equal(redactUrl("https://user:pass@rpc.example/v1?key=secret"),"https://rpc.example/v1");});
