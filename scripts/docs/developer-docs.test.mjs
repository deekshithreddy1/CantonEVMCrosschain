import assert from"node:assert/strict";import test from"node:test";import{readFile}from"node:fs/promises";import path from"node:path";import{fileURLToPath}from"node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../.."),pages=["quick-start","concepts","networks","assets","identity","transfers","bridge-operations","attestations","settlements","workflows","webhooks","errors","idempotency","finality","trust-model","security","testing","runbooks"],sections=["## What it is","## Why it exists","## Example","## Failure cases","## API usage"];for(const page of pages)test(`${page} documents concept, rationale, example, failures, and API`,async()=>{const text=await readFile(path.join(root,"docs/developers",`${page}.md`),"utf8");for(const section of sections)assert.ok(text.includes(section),`${page} missing ${section}`);assert.doesNotMatch(text,/cross-network atomic(?:ity)? is guaranteed/i);});
