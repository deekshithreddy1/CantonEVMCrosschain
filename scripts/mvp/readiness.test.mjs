import assert from"node:assert/strict";import test from"node:test";import{spawnSync}from"node:child_process";import path from"node:path";import{fileURLToPath}from"node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../..");test("MVP report preserves implemented evidence without claiming live completion",()=>{const r=spawnSync(process.execPath,["scripts/mvp/readiness.mjs"],{cwd:root,encoding:"utf8"});assert.equal(r.status,0);const x=JSON.parse(r.stdout);assert.equal(x.status,"MVP_NOT_COMPLETE");assert.equal(x.total,17);assert.equal(x.evidenced,13);assert.deepEqual(x.blocked,["reproducible-local-canton-evm","logical-asset-registered-on-both","canton-to-evm-lock-mint","evm-to-canton-burn-release"]);});test("MVP assertion fails until Digital Asset LocalNet evidence exists",()=>{const r=spawnSync(process.execPath,["scripts/mvp/readiness.mjs","--assert-complete"],{cwd:root,encoding:"utf8"});assert.notEqual(r.status,0);assert.match(r.stderr,/completion blocked/);});
