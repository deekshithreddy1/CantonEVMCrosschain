# Packages

Planned packages: `schemas`, `asset-registry`, `identity`, `policy-engine`, `attestations`, `transaction-engine`, `settlement`, `workflow-engine`, `canton-adapter`, `evm-adapter`, `sdk-typescript`, and `testing`. `core` contains the initial network-neutral model.
