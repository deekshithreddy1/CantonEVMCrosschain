# InterWeave CLI

Set `INTERWEAVE_API_KEY` and optionally `INTERWEAVE_BASE_URL`, then run `interweave networks`, `interweave assets get <id>`, `interweave bridge move --data '{...}'`, or `interweave doctor`. Every command supports `--json` for automation. Secrets are read from the environment, not command arguments.
