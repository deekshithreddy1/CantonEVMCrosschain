# Applications

Planned modules: `api`, `coordinator`, `validator`, `reconciler`, `indexer-evm`, `indexer-canton`, and `dashboard`. Runtime implementations begin after the Phase 0–1 contracts are approved.
