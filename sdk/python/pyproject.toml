[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "interweave-sdk"
version = "0.1.0"
requires-python = ">=3.11"
description = "Python SDK for the InterWeave Canton-EVM control plane"
[tool.setuptools.packages.find]
where = ["src"]
